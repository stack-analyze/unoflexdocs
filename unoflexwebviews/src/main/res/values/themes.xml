<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UnoFlex" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <style name="Theme.Stating" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">5000</item>
        <item name="postSplashScreenTheme">@style/Theme.UnoFlex</item>
        <item name="splashScreenIconSize">128dp</item>
    </style>
</resources>